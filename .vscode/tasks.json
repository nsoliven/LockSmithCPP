{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build and clean",
            "type": "shell",
            "command": "make && make clean",
            "options": {
                "cwd": "${workspaceFolder}/build"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": "$gcc"
        }
    ]
}
